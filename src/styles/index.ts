import styled from 'styled-components';

const WeatherContainer = styled.div`
  display: flex;
  justify-content: center;
  align-items: center;
  padding: 20px;
  background-color: #f0f0f0;
`;

const Temperature = styled.h2`
  font-size: 2rem;
  color: #333;
`;

export { WeatherContainer, Temperature };
